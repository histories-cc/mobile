name: mobile
description: Histories mobile

publish_to: "none" # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.18.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2

  gql_http_link: ^0.4.4+1
  ferry_flutter: ^0.6.1+1

  flutter_map: ^3.0.0
  latlong2: ^0.8.1

dev_dependencies:
  flutter_test:
    sdk: flutter

  ferry_generator: ^0.6.0
  build_runner: ^2.2.0

  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
